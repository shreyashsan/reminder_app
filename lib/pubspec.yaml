name: reminder_app

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^12.0.0 # For local notifications
environment:
  sdk: ^3.2.0
  flutter: ">= 3.22.0"
flutter_lints: ^2.0.1
dev_dependencies:
  flutter_lints: ^2.0.0
